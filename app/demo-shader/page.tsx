import AnimatedShaderBackground from "@/components/ui/animated-shader-background";

export default function DemoShaderPage() {
  return (
    <div style={{ margin: 0, minHeight: "100vh", backgroundColor: "#000" }}>
      <AnimatedShaderBackground />
    </div>
  );
}
