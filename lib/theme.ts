/**
 * Shared theme constants — keep in sync with the inline script in `app/layout.tsx`
 * and future CLI templates that copy theme behavior.
 */
export const THEME_STORAGE_KEY = "my-ui-theme";

export type ThemeMode = "light" | "dark";
